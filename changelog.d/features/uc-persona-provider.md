- **feat(providers): add UC Persona / Emotional (uncensored.com), the subscription-backed persona chat as an OpenAI-compatible provider.** A WebSocket web-app port: a durable Clerk credential mints a short-lived session token per connect (browserless, no API key), driving UC's persona socket. Ships a server-side email-code login endpoint (request, verify, token-mint proof, persist), 19 verified persona models, prompted tool-calling with a pure per-model code-style dialect and bounded auto-cure retry, reasoning split, streaming and non-streaming responses, and quota/auth errors (paywall, message limit, rate limit to 429 with reset cooldown; invalid session to 401). Partial socket or TTS failures fail closed instead of returning truncated success. The capture-backed media paths include vision (15 capable chat models), one-file document RAG with readiness confirmation, image generation (22 models), image-to-video (`wan-2.2-spicy`), and TTS (`model: default`, Jade voice, streaming MP3). Daily account limits apply. Registered as `uc-persona` with legacy alias `uc`; the metered Developer API is the separate `uc-direct` provider.
