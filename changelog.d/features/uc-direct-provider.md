- **feat(providers): add UC Direct / Developer (uncensored.com Developer API), the metered OpenAI-compatible surface.** A standard default-executor passthrough to the complete `https://api.uncensored.com/api/v1/chat/completions` endpoint with `X-api-key` auth, streaming SSE, and native tool-calling. The public no-auth `/models` discovery currently returns 82 models across 15 providers, with the same 82 IDs retained as a static fallback. Separate image and video registry entries route the documented Direct media endpoints through the developer-key connection. Registered as `uc-direct` (`ucd`); its catalog, metered credits, and API-key authentication remain separate from the daily-limited `uc-persona` subscription.
