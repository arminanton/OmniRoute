- **feat(providers): add UC Direct / Developer (uncensored.com Developer API), the metered OpenAI-compatible chat surface.** A minimal default-executor passthrough sends chat completions to `https://api.uncensored.com/api/v1/chat/completions` with `X-api-key` authentication. Public `/models` discovery supplies the chat catalog, with a static chat-model list as fallback when discovery is unavailable. Registered as `uc-direct` (`ucd`); its catalog, metered credits, and API-key authentication remain separate from the plan-backed `uc-persona` subscription.
